# Integration test results

Generated by `npm run integration` on 2026-05-23.

## Summary

- **10 scenarios** run
- **10** matched `expect.json` exactly
- **0** mismatched expectations
- **0** informational only (no `expect.json`)

Each scenario is a self-contained directory shaped like a real Node.js
project — `package.json`, `package-lock.json`, optionally `.npmrc` /
`jadguard.config.json`. Guard runs against it with
`jadguard audit --offline --no-color` unless the scenario overrides via
`args.json`.

## Scenarios

### 01-clean

> Baseline: a known-good project (single exact-pinned popular dep) must pass cleanly.

**Attack vector:** None — control sample.

**Command:** `jadguard audit --offline --no-color`

**Exit code:** 0

**Assessment:** ✅ expectations met

**Output:**

```
JAD Apps Guard v0.1.0 — audit

  No findings.

  1 of 1 dependencies evaluated · npm lockfile

  PASS  (exit 0)

  threat feed: 2026-05-23 · 239 popular packages
```

### 02-no-lockfile

> A project declares deps but commits no lockfile — installs are not reproducible.

**Attack vector:** Floating-range install picks up whatever the registry currently serves. Every poisoned-republish vector relies on resolvers picking up the new version.

**Command:** `jadguard audit --offline --no-color`

**Exit code:** 1

**Assessment:** ✅ expectations met

**Output:**

```
JAD Apps Guard v0.1.0 — audit

  HIGH      No lockfile present (package.json) [no-lockfile]
          The project declares dependencies in package.json but commits no package-lock.json, pnpm-lock.yaml or yarn.lock. Without a lockfile, installs are not reproducible — each install can resolve dependencies to different code — and Guard has no pinned dependency set to inspect.
          -> Generate and commit a lockfile (`npm install`, `pnpm install`, or `yarn install`), and install with `npm ci` or an equivalent frozen-lockfile flag in CI.


  no lockfile — nothing to evaluate

  FAIL — 1 high  (exit 1)
```

### 03-install-scripts-runnable

> Dependency declares an install script; project has no ignore-scripts posture.

**Attack vector:** The primary execution vector for npm supply-chain attacks: a poisoned release of a package with a lifecycle script runs arbitrary code on every machine that installs it. Without ignore-scripts the script will execute on install.

**Command:** `jadguard audit --offline --no-color`

**Exit code:** 1

**Assessment:** ✅ expectations met

**Output:**

```
JAD Apps Guard v0.1.0 — audit

  HIGH      evil-postinstall@1.0.0 declares an install script [install-scripts]
          This dependency declares a preinstall/install/postinstall lifecycle script that executes automatically on install. A single poisoned release of a package with an install script runs arbitrary code on every machine and CI runner that installs it.
          -> Set `ignore-scripts=true` in .npmrc and explicitly allowlist only packages whose install scripts you have reviewed.


  1 of 1 dependencies evaluated · npm lockfile

  FAIL — 1 high  (exit 1)

  threat feed: 2026-05-23 · 239 popular packages
```

### 04-install-scripts-ignored

> Same install-script dependency, but the project sets ignore-scripts=true in .npmrc.

**Attack vector:** Same as scenario 03, with the project's posture mitigating the actual execution. Guard reports at LOW severity (informational) — the package still WOULD execute code if installed without the guard.

**Command:** `jadguard audit --offline --no-color`

**Exit code:** 0

**Assessment:** ✅ expectations met

**Output:**

```
JAD Apps Guard v0.1.0 — audit

  LOW       evil-postinstall@1.0.0 declares an install script [install-scripts]
          This dependency declares a preinstall/install/postinstall lifecycle script. The project sets ignore-scripts, so the script will not run on install — this finding confirms the package would otherwise execute code automatically.
          -> No action required. Only allowlist this package for script execution after reviewing what its install script does.


  1 of 1 dependencies evaluated · npm lockfile

  WARN — 1 low  (exit 0)

  threat feed: 2026-05-23 · 239 popular packages
```

### 05-weak-integrity

> A registry dep is pinned by a collision-prone SHA-1 integrity hash.

**Attack vector:** SHA-1 is collision-prone; a tampered tarball could match the hash. Modern npm always uses sha512.

**Command:** `jadguard audit --offline --no-color`

**Exit code:** 0

**Assessment:** ✅ expectations met

**Output:**

```
JAD Apps Guard v0.1.0 — audit

  MEDIUM    stale-package@1.0.0 is pinned with a weak SHA-1 hash [integrity]
          This dependency is pinned with a SHA-1 integrity hash. SHA-1 is collision-prone and no longer provides meaningful tamper resistance for a tarball.
          -> Re-resolve the lockfile with a current package manager to upgrade this entry to a sha512 integrity hash.


  1 of 1 dependencies evaluated · npm lockfile

  WARN — 1 medium  (exit 0)

  threat feed: 2026-05-23 · 239 popular packages
```

### 06-git-dep

> Dependency resolved from a git source rather than the registry.

**Attack vector:** Git deps skip the publishing pipeline (no integrity hash, no provenance, no advisory feed). The tj-actions/changed-files compromise (CVE-2025-30066) used exactly this technique by retrofitting tags.

**Command:** `jadguard audit --offline --no-color`

**Exit code:** 0

**Assessment:** ✅ expectations met

**Output:**

```
JAD Apps Guard v0.1.0 — audit

  MEDIUM    forked-lib resolves from a git source [git-dep]
          This dependency is installed directly from a git reference rather than the public registry. Git dependencies skip the publishing pipeline — there is no integrity hash, no provenance attestation, and no advisory feed coverage — and a mutable git ref (branch, tag, HEAD) can be rewritten in place, so the same lockfile entry may resolve to different code over time.
          -> If you need a fork or an unreleased fix, pin the git dep to a full commit SHA and mirror the package to a private registry where possible. Replace with a registry release as soon as one is available.


  1 of 1 dependencies evaluated · npm lockfile

  WARN — 1 medium  (exit 0)

  threat feed: 2026-05-23 · 239 popular packages
```

### 07-unpinned-ranges

> package.json mixes caret, tilde, and `latest` ranges. Lockfile pins to specific versions for now.

**Attack vector:** Floating ranges are the exact population that auto-adopts a poisoned republish during the typical hours-long compromise window. Severity is `low` by design; the rule is excluded from `failOn: high` by default.

**Command:** `jadguard audit --offline --no-color`

**Exit code:** 0

**Assessment:** ✅ expectations met

**Output:**

```
JAD Apps Guard v0.1.0 — audit

  LOW       always-fresh is unpinned in package.json (dist-tag "latest") [unpinned-ranges]
          package.json declares "always-fresh": "latest". A floating range lets the next install resolve to whatever the registry currently serves — this is the exact population that auto-adopts a poisoned republish during the typical hours-long window between an account-takeover compromise and its takedown.
          -> Pin the range to an exact version (e.g. "always-fresh": "X.Y.Z"), and install with `npm ci` / `--frozen-lockfile` in CI so the lockfile is authoritative.

  LOW       chalk is unpinned in package.json (tilde (~) range) [unpinned-ranges]
          package.json declares "chalk": "~5.3.0". A floating range lets the next install resolve to whatever the registry currently serves — this is the exact population that auto-adopts a poisoned republish during the typical hours-long window between an account-takeover compromise and its takedown.
          -> Pin the range to an exact version (e.g. "chalk": "X.Y.Z"), and install with `npm ci` / `--frozen-lockfile` in CI so the lockfile is authoritative.

  LOW       lodash is unpinned in package.json (caret (^) range) [unpinned-ranges]
          package.json declares "lodash": "^4.17.21". A floating range lets the next install resolve to whatever the registry currently serves — this is the exact population that auto-adopts a poisoned republish during the typical hours-long window between an account-takeover compromise and its takedown.
          -> Pin the range to an exact version (e.g. "lodash": "X.Y.Z"), and install with `npm ci` / `--frozen-lockfile` in CI so the lockfile is authoritative.


  3 of 3 dependencies evaluated · npm lockfile

  WARN — 3 low  (exit 0)

  threat feed: 2026-05-23 · 239 popular packages
```

### 08-dependency-confusion

> .npmrc declares @company as an internal-registry scope; the lockfile resolved @company/internal-util from the PUBLIC registry instead.

**Attack vector:** Classic dependency-confusion: a private-scope name also exists publicly with a higher version, and the resolver picked up the public (potentially malicious) one.

**Command:** `jadguard audit --offline --no-color`

**Exit code:** 1

**Assessment:** ✅ expectations met

**Output:**

```
JAD Apps Guard v0.1.0 — audit

  HIGH      @company/internal-util@1.0.0 resolves from registry.npmjs.org, not the internal registry for @company [dependency-confusion]
          `.npmrc` declares `@company:registry=https://npm.internal.example.com/`, but the lockfile pulled this package from registry.npmjs.org. This is the dependency-confusion attack pattern: a private-scope name also exists publicly, and the resolver picked the public (potentially malicious) package over the intended internal one.
          -> Force @company/internal-util to resolve from the internal registry. Configure `always-auth` in `.npmrc` for the internal registry, and re-resolve the lockfile from a clean install. If this dependency genuinely should come from the public registry, drop the scope from `.npmrc` or move the package to a non-internal name.


  1 of 1 dependencies evaluated · npm lockfile

  FAIL — 1 high  (exit 1)

  threat feed: 2026-05-23 · 239 popular packages
```

### 09-typosquat-experimental

> Two typosquatted package names installed: `lodahs` (transposition of `lodash`, distance 1) and `raect` (transposition of `react`, distance 1).

**Attack vector:** The entry vector used by the August 2025 10-package credential-harvester campaign and the typosquat that seeded the Axios DPRK case. Experimental rule, opted into via jadguard.config.json.

**Command:** `jadguard audit --offline --no-color`

**Exit code:** 0

**Assessment:** ✅ expectations met

**Output:**

```
JAD Apps Guard v0.1.0 — audit

  MEDIUM    lodahs@1.0.0 looks like a typosquat of "lodash" [typosquat]
          The package name "lodahs" is edit-distance 1 from the popular package "lodash". Typosquat-style names are the entry vector used by the August 2025 10-package credential-harvester campaign and the typosquat that seeded the Axios DPRK case.
          -> Check whether you meant "lodash". If "lodahs" really is a legitimate package you intended to install, suppress this finding via the `ignores` config with a brief justification.

  MEDIUM    raect@1.0.0 looks like a typosquat of "react" [typosquat]
          The package name "raect" is edit-distance 1 from the popular package "react". Typosquat-style names are the entry vector used by the August 2025 10-package credential-harvester campaign and the typosquat that seeded the Axios DPRK case.
          -> Check whether you meant "react". If "raect" really is a legitimate package you intended to install, suppress this finding via the `ignores` config with a brief justification.


  2 of 2 dependencies evaluated · npm lockfile

  WARN — 2 medium  (exit 0)

  threat feed: 2026-05-23 · 239 popular packages
```

### 10-self-integrity-tampered

> Config attempts to disable, downgrade, AND ignore the non-suppressible self-integrity rule. mode is `warn` to test that the non-suppressible finding still fails the verdict.

**Attack vector:** The first thing a compromised project (or developer under deadline pressure) would try: silently switch off the gate. Strategy makes this load-bearing: it must produce a CRITICAL non-suppressible finding that fails the verdict even in warn mode.

**Command:** `jadguard audit --offline --no-color`

**Exit code:** 1

**Assessment:** ✅ expectations met

**Output:**

```
JAD Apps Guard v0.1.0 — audit

  CRITICAL  Configuration attempts to disable the protected rule "self-integrity" [self-integrity]
          Guard found configuration that tries to weaken one of its own non-suppressible protections. Non-suppressible rules exist so that a compromised project — or a developer under deadline pressure — cannot quietly switch off the gate. This finding itself cannot be suppressed.
          -> Remove the offending entry from your Guard configuration. If a non-suppressible rule produces a genuine false positive, report it rather than disabling it.

  CRITICAL  Configuration attempts to downgrade the protected rule "self-integrity" to low [self-integrity]
          Guard found configuration that tries to weaken one of its own non-suppressible protections. Non-suppressible rules exist so that a compromised project — or a developer under deadline pressure — cannot quietly switch off the gate. This finding itself cannot be suppressed.
          -> Remove the offending entry from your Guard configuration. If a non-suppressible rule produces a genuine false positive, report it rather than disabling it.

  CRITICAL  Configuration attempts to ignore findings from the protected rule "self-integrity" [self-integrity]
          Guard found configuration that tries to weaken one of its own non-suppressible protections. Non-suppressible rules exist so that a compromised project — or a developer under deadline pressure — cannot quietly switch off the gate. This finding itself cannot be suppressed.
          -> Remove the offending entry from your Guard configuration. If a non-suppressible rule produces a genuine false positive, report it rather than disabling it.


  1 stale ignore entry (matched nothing or expired) — clean these up.

  1 of 1 dependencies evaluated · npm lockfile

  FAIL — 3 critical  (exit 1)

  threat feed: 2026-05-23 · 239 popular packages
```
