# JADSecure dogfoods its own guidance: lifecycle scripts are the primary
# supply-chain execution vector, so they are disabled for this repository.
ignore-scripts=true

# Reproducible installs: never silently mutate the committed lockfile.
save-exact=true
